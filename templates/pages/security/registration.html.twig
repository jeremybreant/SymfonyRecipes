{% extends 'base.html.twig' %}

{% block title %}SymfonyRecipes - Inscription{% endblock %}
{% block body %}
    <div class="container">
        <h1 class="mt-4">Formulaire d'inscription'</h1>
        {#
        {% if error %}
            <div class="alert alert-dismissible alert-danger mt-4">
                <strong>Erreur :</strong> {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}
        #}

        {{ form(form) }}

    </div>
{% endblock %}